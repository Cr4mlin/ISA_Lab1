<local:BaseView x:Class="WpfView.Views.RegistrationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:WpfView.Views"
        Title="Регистрация" Height="500" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Text="Регистрация"
                   FontSize="24" FontWeight="Bold"
                   HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <!-- Имя пользователя -->
        <TextBlock Grid.Row="1" Text="Имя пользователя:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="2" Text="{Binding NickName, UpdateSourceTrigger=PropertyChanged}"
                 FontSize="14" Padding="5"/>

        <!-- Логин -->
        <TextBlock Grid.Row="3" Text="Логин:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="4" Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}"
                 FontSize="14" Padding="5"/>

        <!-- Пароль -->
        <TextBlock Grid.Row="5" Text="Пароль:" VerticalAlignment="Center"/>
        <PasswordBox Grid.Row="6" x:Name="PasswordBox"
                     FontSize="14" Padding="5"
                     PasswordChanged="PasswordBox_PasswordChanged"/>

        <!-- Сообщение об ошибке -->
        <TextBlock Grid.Row="7" Text="{Binding ErrorMessage}"
                   Foreground="Red" TextWrapping="Wrap"
                   Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>

        <!-- Кнопки -->
        <StackPanel Grid.Row="8" Orientation="Horizontal"
                    HorizontalAlignment="Center" Margin="0,20,0,0">
            <Button Content="Зарегистрироваться" Width="150" Height="35"
                    Margin="0,0,10,0"
                    Command="{Binding RegisterCommand}"
                    IsDefault="True"/>
            <Button Content="Уже есть аккаунт" Width="150" Height="35"
                    Command="{Binding BackToLoginCommand}"/>
        </StackPanel>
    </Grid>
</local:BaseView>
